{
  "$comment": "The oracle + writer: write_specs validates the drafts (spec_check) and writes them atomically into the workspace, bootstrapping go.mod/specs. Invalid -> one repair.",
  "id": "spec.write",
  "kind": "action",
  "tool": "write_specs",
  "inputs": [
    { "from": "$workspace", "path": ".", "as": "proj" },
    { "from": "spec.generate", "path": ".", "as": "code" }
  ],
  "body": { "proj": "{{ proj }}", "code": "{{ code }}" },
  "on_success": "spec.done",
  "on_failure": "spec.fix"
}
