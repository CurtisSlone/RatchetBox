Your previous pitfalls entry was malformed. Return a CORRECTED entry. Fix exactly what the validator
reported. Output ONLY the markdown.

It MUST have: an H1 `# Pitfall: <title>` heading, and at least one ```go code block showing the wrong
code and the fix.

## Failure class
{{ klass }}

## Validator error
{{ errors }}

## Previous attempt
{{ prev }}
