{
  "$comment": "Plan-routing (cpp-style): the model emits one search query per knowledge library, or an empty string to skip it. The next node (test.generate) routes each non-empty query into that library's search, so only the relevant libraries are consulted. On a malformed plan, proceed to generate ungrounded rather than abort.",
  "id": "test.plan",
  "kind": "generate",
  "prompt": "./prompt.md",
  "inputs": [
    { "from": "$input", "path": ".", "as": "task" }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "stdlib_q":     { "type": "string", "description": "a Go standard-library package/symbol query, or empty" },
      "patterns_q":   { "type": "string", "description": "a design-pattern OR algorithm-pattern query, or empty" },
      "guidelines_q": { "type": "string", "description": "an idiomatic-style query (Effective Go / Code Review Comments), or empty" },
      "pitfalls_q":   { "type": "string", "description": "a builds-but-wrong trap query, or empty" },
      "idioms_q":     { "type": "string", "description": "a Go-idioms query, or empty" }
    },
    "required": ["stdlib_q", "patterns_q", "guidelines_q", "pitfalls_q", "idioms_q"]
  },
  "on_success": "test.generate",
  "on_failure": "test.generate"
}
